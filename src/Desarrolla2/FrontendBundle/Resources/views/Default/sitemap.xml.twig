<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for item in most_hits %}
        <url>
            <loc>{{ homepage }}/s?s/[q]={{ item | url_encode() }}</loc>
            <changefreq>daily</changefreq>
        </url>
    {% endfor %}
        {% for item in latest %}
        <url>
            <loc>{{ homepage }}/s?s/[q]={{ item | url_encode() }}</loc>
            <changefreq>daily</changefreq>
        </url>
    {% endfor %}    
</urlset>