<form class="form-search" action="{{ path('search') }}" method="get" {{ form_enctype(form) }}>   
    {{ form_widget(form.q) }}
    <button type="submit" class="btn">Search</button>
    {{ form_rest(form) }}
</form>